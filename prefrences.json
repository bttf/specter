exports.prefrences = {
	
	"paginationSize" :11, 
	 "databaseUrl"   :,
	"searchResults" :,
	"pageFields":[],
	"summaryLength" : ,
	
};


								//  REFERENCE

//paginationSze : Gives the number of pages in the index page and the search page. Should be an integer and one more than actual number of pages you want to display .For example if you want to display 10 pages use a pagination size of 11.

// databaseUrl : The url that points yo your elastic search database. Could be local (http://localhost:9200/) or on the web.

// Search results : number of results to display in the search box. This is just for the text box autocomplete. On a different page number of results is equal to the pagination size


//pageFileds : The fields that you want to display on the index page. The array can contain following items for the default schema. 

// postHtml, postedBy, postedOn, title, wordCount



//summaryLength : Number of html elements to display in the summary of the index. If 0 no items are displayed. If one the first html element is displayed and so on.

